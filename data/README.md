# data/

Esta carpeta se crea automáticamente en tiempo de ejecución.
Su contenido está excluido del repositorio (.gitignore).

Archivos generados aquí:
- incidentes.db     — base de datos SQLite de incidentes
- secret.key        — clave de cifrado Fernet (NUNCA compartir)
- reporte_*.pdf     — reportes exportados por el operador
