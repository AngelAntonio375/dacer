# DaCer — Dependencias Python
# Instalar con: pip install -r requirements.txt
#
# IMPORTANTE: PyTorch con CUDA debe instalarse por separado si pip descarga la versión CPU:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# ─── Interfaz gráfica ────────────────────────────────
PyQt6==6.7.0

# ─── Visión por computadora ──────────────────────────
opencv-python==4.10.0.84
numpy==1.26.4
Pillow==10.3.0

# ─── Inferencia YOLOv8 ───────────────────────────────
ultralytics==8.2.0
torch==2.3.0
torchvision==0.18.0

# ─── Base de datos y cifrado ─────────────────────────
# sqlite3 viene incluido en Python (stdlib) — no se instala aquí
cryptography==42.0.8

# ─── Generación de reportes PDF ──────────────────────
reportlab==4.2.2

# ─── Pruebas automatizadas ───────────────────────────
pytest==8.2.2
